# Day7: AI動画生成

添付資料から差し替えた `images/complete-01.png` から `images/complete-09.png` を前半に、既存の `images/1.avif` から `images/6.avif` を後半に表示する15ページのスライドです。

`index.html` をブラウザで開くと、スクロール、目次、前後ボタン、またはキーボードの左右キーでスライドを切り替えられます。
